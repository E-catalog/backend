# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: exdoample
      POSTGRES_USER: example
      POSTGRES_DB: mydatabase
    ports:
      - 5432:5432
